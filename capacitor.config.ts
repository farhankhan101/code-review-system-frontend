import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aicodereviewsystem.app',
  appName: 'Ai Powered Code Review System',
  webDir: 'dist'
};

export default config;
